# TradingBand
Do simulations to study optimal trading bands in a trading system to maximize Sharpe ratio after transaction costs. `compile.bat` compiles the Fortran program. Run the Python program with `python xtrading_band.py`.
